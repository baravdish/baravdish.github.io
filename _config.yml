# theme: minima
title: The small things in life
description: "A simple blog about my thoughts and projects."
remote_theme: mmistakes/minimal-mistakes
# Set default values for layouts and posts
defaults:
  - scope:
      path: ""
      type: posts
    values:
      layout: single
      author_profile: true # We'll use a custom sidebar instead
      sidebar:
        - title: "Highlights"
          html: "{% include sidebar.html %}"

